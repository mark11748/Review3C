@{Layout="_Layout";}
<br><br>
<br><br>
<p>Here are all currently working stylists:</p>
<p>----------------------------------------</p>
<ul>
  @if(Model != null && Model.Count != 0)
  {
    foreach(var a in Model)
    { <li> <a href="stylists/@a.GetId()/clients">@a.GetName() : @a.GetId()</a> </li> }
  }
</ul>
<p>----------------------------------------</p>
<br>
<br>
<br>
<p>Click <a href="/stylists/new">HERE</a> to add a stylist to the list.</p> <br>
<p>Click <a href="/stylists/delete">HERE</a> to remove all stylists from the list.</p> <br>
